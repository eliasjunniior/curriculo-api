# --- Database Configuration (Development using Docker) ---
spring.datasource.url=jdbc:postgresql://localhost:5432/curriculos_db
# --- Use as MESMAS credenciais do docker-compose.yml ---
spring.datasource.username=eliasj_dev_user
spring.datasource.password=EliasDevP@ss1!
# ---

# --- JPA/Hibernate Configuration ---
# update: Tenta atualizar o schema. Bom para desenvolvimento.
spring.jpa.hibernate.ddl-auto=update

# Mostra o SQL gerado no console (bom para debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Define o dialeto correto para PostgreSQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# --- Server Configuration ---
server.port=8080 # Porta padrão

# --- Jackson Configuration (para Datas - opcional mas bom) ---
# spring.jackson.serialization.write-dates-as-timestamps=false
# spring.jackson.date-format=yyyy-MM-dd